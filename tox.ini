[tox]
envlist = py39, py310, py311, py312, py313, pypy3

[testenv]
deps =
    pytest>=7.0.0
    pytest-cov>=4.0.0
setenv =
    PYTHONUNBUFFERED = 1
commands =
    pytest {posargs:tests/ -vv --tb=short --capture=no}
